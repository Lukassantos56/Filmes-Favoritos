# Copie este arquivo para .env e preencha (uso local)

DB_USER=postgres
DB_HOST=localhost
DB_PASSWORD=sua_senha_aqui
DB_NAME=bancoFilme
DB_PORT=5432

# Na nuvem (Railway, Neon, Render etc.), use DATABASE_URL em vez dos campos acima:
# DATABASE_URL=postgresql://usuario:senha@host/banco?sslmode=require
